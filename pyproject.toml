[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "comms"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["cbor2", "PyNaCl", "blake3"]

[tool.setuptools.packages.find]
where = ["."]
include = ["comms*"]
